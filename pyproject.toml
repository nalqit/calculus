[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "calculus"
version = "0.1.0"
description = "A from-scratch Python library for symbolic and numerical calculus operations"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "Calculus Library Contributors"},
]
keywords = ["calculus", "symbolic", "differentiation", "integration", "mathematics"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Education",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Mathematics",
]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-cov", "ruff"]
numpy = ["numpy>=1.24"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=calculus --cov-report=term-missing --cov-report=html"

[tool.coverage.run]
source = ["calculus"]
omit = ["calculus/tests/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "def __repr__",
    "@abstractmethod",
]
fail_under = 80

[tool.ruff]
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]
